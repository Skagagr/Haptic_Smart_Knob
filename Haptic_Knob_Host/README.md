# 力反馈旋钮上位机（Haptic_Knob_Host）

基于 **C# WinForms (.NET 8)** 的 PC 端上位机，通过 USB-CDC 虚拟串口与 STM32 固件通信，
验证与调试固件的双向通信协议（帧头 + 长度 + CRC8 校验）。

---

## 已实现功能

### 串口通信（USB-CDC）
- 打开 / 关闭串口（串口号当前硬编码为 COM7）
- 50ms 定时轮询查询角度，转动旋钮实时刷新

### 查询角度
- 自动发送 `GET_ANGLE` 命令帧，解析回包中的角度（float 小端）
- 界面实时显示角度数值

### 预设切换
- 5 种预设一键切换（COARSE_6 / NORMAL_12 / FINE_24 / DENSE_48 / SMOOTH）
- 发送 `SET_CONFIG` 命令，旋钮运行中实时切换卡位手感
- 显示 ACK 结果（成功 / 状态码）

### 收发日志
- 时间戳 + hex 帧显示（发送帧与接收帧）

---

## 软件架构

### 分层设计

```
┌────────────────────────────────────────────┐
│  Form1.cs          界面层                  │
│  - 串口开关 / 定时轮询 / 角度显示            │
│  - 预设按钮 / ACK 显示 / 收发日志           │
└───────────────┬────────────────────────────┘
                │
┌───────────────▼────────────────────────────┐
│  KnobProtocol.cs   协议层                  │
│  - CRC8 计算（多项式 0x07）                 │
│  - 帧构建 BuildFrame / 具体命令封装          │
│  - KnobFrameParser 逐字节解析状态机          │
└────────────────────────────────────────────┘
```

### 文件结构

```
Haptic_Knob_Host/
├── Form1.cs                  # 主窗口（界面 + 事件逻辑）
├── Form1.Designer.cs         # 设计器布局（VS 自动生成）
├── Form1.resx
├── Program.cs                # 程序入口
├── Haptic_Knob_Host.csproj   # 工程文件（.NET 8 + System.IO.Ports）
└── Protocol/
    └── KnobProtocol.cs       # 协议层：CRC8 + 组帧 + 帧解析
```

### 协议说明

帧格式与固件 `app_usb_protocol.h` 一致：

```
[0xAA][0x55][Type][Len][Payload 0~255B][CRC8]
```

| 命令 | 类型 | 载荷 | 响应 | 说明 |
|------|------|------|------|------|
| `GET_ANGLE` | 0x03 | 空 | 0x83 | 查询角度，回 4B float（小端） |
| `SET_CONFIG` | 0x02 | 1B = preset (0~4) | 0x82 | 设置预设 |

- 同步头：`0xAA 0x55` 双字节
- CRC8：多项式 0x07，覆盖 Type+Len+Payload
- 响应类型 = 命令类型 | 0x80，响应载荷首字节为状态码

---

## 快速开始

1. Visual Studio 打开 `Haptic_Knob_Host.sln`（.NET 8 桌面开发工作负载）
2. 旋钮 USB 插入电脑，在设备管理器确认虚拟串口号（如 COM7）
3. 若串口号不是 COM7，修改 `Form1.cs` 中 `new SerialPort("COM7", 115200)`
4. F5 运行 → 打开串口 → 自动轮询显示角度 → 点击预设按钮切换卡位手感

> 依赖 NuGet 包：`System.IO.Ports`

---

## 更新日志

### v0.2.1 (2026-08-05)
- 代码重构：协议魔法数字抽为枚举（KnobCmd / KnobStatus），响应判断语义化
- Form1 按职责分 region（串口/轮询/发送/解析/命令/日志），抽公共 Send/Log 方法
- 注释精简为方法级摘要，消除重复代码

### v0.2.0 (2026-08-05)
- 协议层 KnobProtocol.cs：CRC8 计算 + 帧构建 + 帧解析状态机
- 接入解析器，自动解析角度并实时显示
- 50ms 定时轮询查询角度
- 预设切换（5 种预设 + ACK 显示）

### v0.1.0 (2026-08-04)
- 初始版本：基础串口收发（打开/关闭串口 + 手动查询 + hex 日志）
